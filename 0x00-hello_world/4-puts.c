#include <stdio.h>
/**
 * main - A program where we use the puts function to print a line
 * we are not allowd to use printf in this code
 * Return: 0 (Sucess)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
